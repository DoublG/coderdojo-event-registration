# Production runtime dependencies: what the running site imports (installed on
# the Level27 server by scripts/deploy.sh). Local development uses
# requirements-dev.txt, which includes this file.
#
# daphne and django-debug-toolbar are here even though they're dev tools:
# website/settings.py lists both in INSTALLED_APPS unconditionally, so the
# site can't start without them (the toolbar is inactive with DEBUG off).
#
# Direct dependencies:
channels==4.3.2
channels_redis==4.3.0
daphne==4.2.3
django-debug-toolbar==8.0.0
django-environ==0.14.0
django-redis==7.0.0
Django==6.1.1
Markdown==3.10.3
mysqlclient==2.3.0
pillow==12.3.0
redis==8.1.0
requests==2.34.2

# Pulled in by the above (pinned for reproducible installs):
asgiref==3.12.1
attrs==26.1.0
autobahn==26.7.1
Automat==25.4.16
cbor2==6.1.4
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
constantly==23.10.4
cryptography==50.0.1
hyperlink==21.0.0
idna==3.20
Incremental==24.11.0
msgpack==1.2.2
packaging==26.3
pycparser==3.0
pyOpenSSL==26.4.0
service-identity==26.1.0
sqlparse==0.6.0
Twisted==26.4.0
txaio==26.6.1
typing_extensions==4.16.0
ujson==6.0.0
urllib3==2.8.0
zope.interface==8.6

# background jobs
celery==5.6.3
django_celery_results==2.6.0
# django-celery-beat 2.9.0 (the latest release) caps Django<6.1; upstream main
# allows <6.2 since celery/django-celery-beat#1042 but hasn't released it yet.
# Pinned to that commit as a tarball (no git needed on the server). Swap back
# to a plain `django-celery-beat==X.Y.Z` once a release supports Django 6.1.
django-celery-beat @ https://github.com/celery/django-celery-beat/archive/dde53dcbe2bdd80c9e818fb6119523f5f39ab8e4.tar.gz

# Pulled in by the background-job packages (pinned for reproducible installs):
amqp==5.4.0
billiard==4.3.0
click==8.5.0
click-didyoumean==0.3.1
click-plugins==1.1.1.2
click-repl==0.4.0
cron_descriptor==2.1.0
django-timezone-field==7.2.2
kombu==5.6.2
prompt_toolkit==3.0.53
python-crontab==3.4.0
python-dateutil==2.9.0.post0
six==1.17.0
tzdata==2026.4
tzlocal==5.4.4
vine==5.1.0
wcwidth==0.9.1

# The external registration/attendance API (website/api.py), and what it pulls in:
django-ninja==1.7.1
pydantic==2.13.5
pydantic_core==2.46.5
annotated-types==0.8.0
typing-inspection==0.4.4
